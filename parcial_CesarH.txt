
PART II: gghg

*CREATE TABLE CATEGORIES
( ID INT,
 NAME  VARCHAR2(255),
 SEASON  VARCHAR2(255),
 CONSTRAINT ID_PK PRIMARY KEY (ID),
 CONSTRAINT SEASON_COULD CHECK (SEASON IN ('winter', 'summer', 'spring', 'autumn')));
 
*CREATE TABLE PRODUCTS
( ID INT,
 NAME VARCHAR2(255),
 REFERENCE VARCHAR2(255),
 PRICES DECIMAL(10,3),
 CATEGORY_ID INT,
 CONSTRAINT ID_PRODUC_PK PRIMARY KEY (ID),
 CONSTRAINT PRICEVALOR CHECK (PRICES>0),
 CONSTRAINT PRODUCTS FOREIGN KEY (CATEGORY_ID) REFERENCES CATEGORIES(ID));


*CREATE TABLE CATEGORIES
( ID INT,
 NUMBER_OF_QUESTION  VARCHAR2(255),
 ANSWER  VARCHAR2(255),



*CREATE TABLE CATEGORIES
( ID INT,
 NAME  VARCHAR2(255),
 SEASON  VARCHAR2(255),
 CONSTRAINT ID_PK PRIMARY KEY (ID),
 CONSTRAINT SEASON_COULD CHECK (SEASON IN ('winter', 'summer', 'spring', 'autumn')));
 

*CREATE TABLE PRODUCTS
( ID INT,
 NAME VARCHAR2(255),
 REFERENCE VARCHAR2(255),
 PRICES DECIMAL(10,3),
 CATEGORY_ID INT,
 CONSTRAINT ID_PRODUC_PK PRIMARY KEY (ID),
 CONSTRAINT PRICESVALORES CHECK (PRICES>0),
 CONSTRAINT PRODUCTS FOREIGN KEY (CATEGORY_ID) REFERENCES CATEGORIES(ID));


*CREATE TABLE ANSWERS
( ID INT,
 NUMBER_OF_QUESTION  VARCHAR2(255),
 ANSWER  VARCHAR2(255));


ANSWERS:


PART III:


1.CREATE TABLESPACE HACEB DATAFILE
'C:\oraclexe\app\oracle\oradata' SIZE 100M,
AUTOEXTEND ON NEXT 100M MAXSIZE 300M
EXTEND MANAGMENT LOCAL
SEGMENT SPACE MANAGMENT AUTO
ONLINE;


2.CREATE PROFILE VENTAS LIMIT
CONNECT_TIME 18
FAILED_LOGIN_ATTEMPTS 3
SESSIONs_PER_USER 1;


3.CREATE USER AMARTINEZ
IDENTIFIED BY amartinez
DEFAULT TABLESPACE HACEB
QUOTA  UNLIMITED ON HACEB
PROFILE VENTAS



4.GRANT CONNECT TO AMARTINEZ;
  GRANT BDA TO AMARTINEZ;








